[pytest]
; This suite is 100% function-based tests. Without this, pytest's default
; class-discovery (anything named Test*) mistakes domain models like
; models.test_case.TestCase/TestAspect for test classes and warns about it.
python_classes =
